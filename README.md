# TODO

- [ ] Find a way to store the python packages with their version
- [ ] Symlink them to the venv
- [ ] Lock file to store the installed packages
    - [ ] Also allow for generating a requirements.txt for pip users
- [ ] Allow switching versions for packages and update the dependency versions
- [ ] LSP to import packages
